{% extends 'base.html.twig' %}

{% block title %}Velvet
{% endblock %}

{% block body %}
	<div class="contenu">
		<h1 class="row col-12">Tous nos disques</h1>
					</li>
						<li class="nav-item text-orange">
				<a class="nav-link text-center" href="{{ path('app_artists') }}">Retour au catalogue des Artistes</a>
			</li>


		<main class="container">
			<div class="row">

			{# {{ artist.name}}</h1> #}
								{# <a href="{{ path('app_discs_artist', {'artist_id': artist.id}) }}">
                            {{ artist.name}} #}

			<!-- <a href="add_form.php" class="btn btn-primary float-end">Ajouter</a> -->
					{% for disc in discs %}
						<div class="col-md-6 mb-4">
							<div
								class="card" style="width: 18rem;">
								<img src="{{ asset('img/' ~ disc.picture) }}" alt="{{ disc.title }}">
								<div class="card-body">
									<h5 class="card-title">
										{{ disc.title }}</h5>
									<p class="content bg-body">
										{{ disc.prix}} euros
									</p>
								</div>
							{# Pour la commande #}
							{# <div class="card-footer mt-2">
															<form action="{{ path('panier_ajouter', {'id': plat.id}) }}" method="post">
																	<div class="d-flex justify-content-center"> 
																	<button class="btn btn-secondary bg-danger quantity-button" href="{{ path('panier_retirer', {id: plat.id }) }}" data-plat-id="{{ plat.id }}" data-action="decrement">-</button>
																	<input name="quantite" value="1" min="1" max="10" class="quantity-input col-2 text-center" data-plat-id="{{ plat.id }}" type="number">
																	<button class="btn btn-secondary bg-success quantity-button" href="{{ path('panier_ajouter', {id: plat.id }) }}" data-plat-id="{{ plat.id }}" data-action="increment">+</button>
																</div> #}

							{# vers panier #}
								<button type="submit" class=" d-block mt-5 btn btn-success mx-auto mb-3" href="{{ path('panier_ajout', {id: disc.id})}}"
> Ajouter au panier
								{# <i class="bi bi-basket"></i> #}
							</button>

						</form>
					</div>
				</div>
			</div>
			{# Si vous avez affiché 2 cartes, fermez la rangée actuelle et commencez une nouvelle rangée #}
			{# {% if loop.index is even %}
									</div>
									<div class="row"> {% endif %} #}
								{% endfor %}
		</div>
	</main>
</div>
{% include "partials/footer.php" %}
{% endblock %}
